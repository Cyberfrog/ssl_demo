{
  "name": "ssl_demo",
  "version": "1.0.0",
  "description": "",
  "main": "client.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "SSL_CERT_FILE=./ssl_certs/certificate.pem node  --use-openssl-ca  client.js"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "express": "^4.16.2"
  }
}
